// prisma.config.js (CommonJS)
require('dotenv').config();

module.exports = {
  schema: './prisma/schema.prisma',
  // Do not read process.env.DATABASE_URL here;
  // the schema's env("DATABASE_URL") handles it.
};
